@model aser_electrification.Models.Region

@{
    ViewData["Title"] = "Edit";
}

<hr />
<div class="d-flex justify-content-center align-items-center">
    <div class="col-md-6">
        <form asp-action="Edit">
            <div class="row mt-5">
                <div class="col">
                    <div class="card">
                        <div class="card-header text-black bg-purple-light text-center">Gestion des régions</div>
                        <div class="card-body">
                            <div class="mb-4">
                                <div class="d-flex mb-2">
                                    <h5 class="card-title flex-grow-1 text-center">Formulaire de modification de la région</h5>
                                </div>

                                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                                <input type="hidden" asp-for="ID_Region" />

                                <div class="col-12">
                                    <label asp-for="Nom_Region" class="control-label">Nom Région</label>
                                    <input asp-for="Nom_Region" class="form-control mt-2" />
                                    <span asp-validation-for="Nom_Region" class="text-danger"></span>
                                </div>

                                <div class="col-12 mt-3">
                                    <button type="submit" value="Save" class="btn btn-primary" aria-label="Enregistrer les modifications"><i class="fas fa-save"></i> Enregistrer</button>
                                    <a asp-action="Index" class="btn btn-danger ms-2" aria-label="Retourner à la liste"><i class="fas fa-backspace"></i> Retourner à la liste</a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
