@model aser_electrification.Models.Departement

@{
    ViewData["Title"] = "Details";
}

<hr />
<div class="d-flex justify-content-center align-items-center">
    <div class="col-md-6">
        <div class="card">
            <div class="card-header text-black bg-purple-light text-center">Détails du Département</div>
            <div class="card-body">
                <h4 class="card-title">Informations sur le Département</h4>
                <hr />
                <dl class="row">
                    <dt class="col-sm-4">
                        @Html.DisplayNameFor(model => model.Nom_Departement)
                    </dt>
                    <dd class="col-sm-8">
                        @Html.DisplayFor(model => model.Nom_Departement)
                    </dd>
                    <dt class="col-sm-4">
                        @Html.DisplayNameFor(model => model.Region)
                    </dt>
                    <dd class="col-sm-8">
                        @Html.DisplayFor(model => model.Region.Nom_Region)
                    </dd>
                </dl>
                <div class="d-flex justify-content-between mt-4">
                    <a asp-action="Edit" asp-route-id="@Model?.ID_Departement" class="btn btn-warning">
                        <i class="fas fa-edit"></i> Modifier
                    </a>
                    <a asp-action="Index" class="btn btn-secondary">
                        <i class="fas fa-backspace"></i> Retourner à la liste
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
